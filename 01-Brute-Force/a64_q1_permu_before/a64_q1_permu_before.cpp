#include<algorithm>
#include<iostream>
#include<vector>

int main(void)
{
    std::ios_base::sync_with_stdio(false); std::cin.tie(NULL);

    int n, m;
    int a, b;

    std::cin >> n >> m;

    for (int i = 0; i < m; i++)
    {
        std::cin >> a >> b;
        
    }
}