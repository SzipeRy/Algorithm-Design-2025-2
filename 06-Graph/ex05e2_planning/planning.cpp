#include <iostream>

int main(void)
{
    std::ios_base::sync_with_stdio(false); std::cin.tie(0);

    int n;
    int m;
    std::cin >> n;

    for (int i = 0 ; i < n ; i++)
    {
        std::cin >> m;
        for (int j = 0 ; i < m; j++)
        {
            std:
        }
    }
}