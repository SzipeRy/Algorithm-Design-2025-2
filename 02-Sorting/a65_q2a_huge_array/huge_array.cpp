#include <algorithm>
#include <iostream>
#include <map>
#include <utility>

int main(void)
{
    std::ios_base::sync_with_stdio(false); std::cin.tie(0);

    int n, q;
    int a, b;
    std::map<int,int> array;

    std::cin >> n >> q;
    for (int i = 0; i < n; i++)
    {
        std::cin >> a >> b;
        
    }
}